# Qwen Audit — PropelBD v3 Website Failure

**BRUTAL ROOT‑CAUSE ANALYSIS – 10 HIGH‑LEVERAGE FAILures**

| # | Failure (what’s broken) | Why it drags the whole experience down | File / line reference (approx.) | Actionable fix (single‑sentence) |
|---|--------------------------|---------------------------------------|--------------------------------|-----------------------------------|
| 1 | **“Hero‑only‑text” layout** – the hero is a single headline + CTA with a huge empty canvas on the opposite side. | Leaves the page looking half‑baked; users get no visual cue that the product is a *system* rather than a static brochure. | `app/velocity-gap/page.tsx` (hero grid, `lg:col-span‑5` for the canvas) | Replace the empty canvas with a purposeful, high‑contrast data‑visual (e.g., animated pipeline graph) that fills the right‑hand column and matches the brand palette. |
| 2 | **Over‑clamped typography** – aggressive `clamp()` + ultra‑tight line‑height & tracking on hero headings. | Text collides, breaks readability, and makes the design feel “generated” rather than thoughtfully tuned. | `tailwind.config.ts` → `fontSize.hero` (lineHeight 0.98, tracking ‑0.02em) used in `DealRoomContent`, `VelocityGapContent`, `OpenTheDoorsContent`. | Adopt a more generous `leading-[1.05]` and `tracking-tight` (‑0.02 em) and remove the 14‑ch max‑width; enable `text-balance` for natural line breaks. |
| 3 | **Copy‑pasted “feature‑list” cards** – four equal‑width cards for Scout/Position/Open/Close. | Turns a process narrative into a sterile grid; no visual hierarchy, no sense of progression, looks like a generic UI kit. | `components/deal-room/DealRoomContent.tsx:48‑63` (grid of 4 cards) | Convert the sequence into a horizontal timeline with a connecting line, staggered card offsets, and progressive animation that tells a story. |
| 4 | **Generic “bento‑board” dashboard** – fake CRM cards that add noise but no value. | Reinforces the “AI‑slop” vibe: a decorative data dump that never earns trust. | `components/deal-room/BentoBoard.tsx:19‑51` (mentioned in critique) | Remove the board or replace it with a single, real‑world case study snapshot (e.g., a signed contract thumbnail) that demonstrates outcome. |
| 5 | **Mis‑used “door” metaphor** – plain rectangular div with a 12 px circle knob. | Looks like a PowerPoint placeholder; fails to evoke a premium, tactile experience. | `components/open-the-doors/OpenTheDoorsContent.tsx:53` (door div) | Swap the CSS rectangle for a hand‑drawn SVG door with subtle inner‑shadow, perspective, and a brushed‑metal handle; animate it opening on scroll. |
| 6 | **Vertical‑rl headline on mobile** – rotated text that forces users to tilt their phones. | Immediate usability break; signals a lack of user‑first thinking and fuels the “AI‑generated” perception. | `components/open-the-doors/OpenTheDoorsContent.tsx:44` (writing‑mode‑vertical) | Delete the vertical writing‑mode; keep the headline horizontal across all breakpoints and adjust size for mobile. |
| 7 | **Monochrome‑heavy, low‑contrast visuals** – faint kinetic “TrajectoryField” canvas, barely visible lines. | The hero feels empty; users can’t discern any meaning, so the design looks like a filler generated by a script. | `components/velocity-gap/TrajectoryField.tsx` (low‑alpha lines) | Increase line opacity to 0.3‑0.4, add subtle glow or mix‑blend, and label key nodes; make the canvas a focal point, not a background whisper. |
| 8 | **Palette misuse – too many accent shades** – `text-muted`, `text-accent`, `text-accent‑light`, `text-ink` all appear side‑by‑side with no hierarchy. | Dilutes brand identity; the eye cannot quickly locate primary actions, reinforcing a “template‑generated” look. | `tailwind.config.ts` → `colors` (multiple accent variants) used across Nav, buttons, and status labels. | Consolidate