import type { Metadata } from "next";
import GallerySite from "@/components/GallerySite";
export const metadata: Metadata = { title: "PropelBD — The quiet art of filled calendars." };
export default function Page() { return <GallerySite />; }
