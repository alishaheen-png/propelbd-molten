import type { Metadata } from "next";
import MoltenUltra from "@/components/MoltenUltra";
export const metadata: Metadata = { title: "PropelBD — Heat makes pipeline." };
export default function Page() { return <MoltenUltra />; }
