import type { Metadata } from "next";
import RoyalSite from "@/components/RoyalSite";
export const metadata: Metadata = { title: "PropelBD — Revenue, conducted with intent." };
export default function Page() { return <RoyalSite />; }
