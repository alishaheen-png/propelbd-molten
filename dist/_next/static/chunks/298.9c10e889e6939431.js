"use strict";(self.webpackChunk_N_E=self.webpackChunk_N_E||[]).push([[298],{2298:function(e,t,n){n.r(t),n.d(t,{default:function(){return u}});var a=n(7437),o=n(2265),i=n(2079),r=n(1448),s=n(6498),l=n(9498);function u(){let e=(0,o.useRef)(null);return(0,o.useEffect)(()=>{var t;let n;if(!e.current||window.matchMedia("(prefers-reduced-motion: reduce)").matches)return;let a=e.current;try{n=new r.CP7({antialias:!1,alpha:!0,powerPreference:"high-performance"})}catch(e){return}if(n.capabilities.maxVertexTextures<1){n.dispose();return}s.ZP.registerPlugin(l.i);let o=window.matchMedia("(pointer: coarse)").matches||window.innerWidth<768,u=o?26e3:82e3,h=Math.ceil(u/512),m=Math.min(window.devicePixelRatio||1,1.5);n.setPixelRatio(m),n.setSize(window.innerWidth,window.innerHeight),n.setClearColor(0,0),a.appendChild(n.domElement);let d=new i.xsS,c=new i.cPb(50,window.innerWidth/window.innerHeight,.1,60);c.position.set(0,0,7.2);let f=function(e,t,n){let a=new Float32Array(512*n*40),o=()=>(Math.random()+Math.random()+Math.random()-1.5)*.8,i=(e,t,o,i,r,s)=>{let l=(512*e*n+t)*4;a[l]=o,a[l+1]=i,a[l+2]=r,a[l+3]=s};for(let t=0;t<e;t++){let e=Math.random();{let e=Math.random(),n=e<.03?1.05+.55*Math.random():e<.22?.65+.5*Math.random():.12+.26*Math.pow(Math.random(),3);i(0,t,5.9*o(),3.35*o(),3.5*o()-.6,n)}i(1,t,6.4*o(),4*o(),3.2*o()-.6,.68>Math.random()?-(.15+.2*Math.random()):.2+.25*Math.random());{let e=Math.pow(Math.random(),1.7);i(2,t,-3.4*(1-e)+o()*(.3+(1-e)*.7),o()*(.35+(1-e)*1.9),.4*o()-.2,.15+1.1*e)}{let e=Math.random(),n=2.6-5*e;i(3,t,(Math.random()-.5)*2*((1-Math.pow(e,.7))*2.6+.04),n,.22*o()-.3,.18+.85*e)}{let e=Math.floor(9*Math.random()),n=e%3==0,a=Math.random(),r=(e/8-.5)*3.2+.12*o();i(4,t,-.6+a*(2.6+e%4*.3),r+-(.35*Math.sin(a*Math.PI)),.1*o()-.3,(n?1.15:.4)*(.5+.7*a))}if(e<.55){let e=Math.random();i(5,t,.22*o(),(e-.5)*4.6,.2*o()-.25,.8+.5*e)}else{let e=.6+3.4*Math.pow(Math.random(),1.5);i(5,t,(.5>Math.random()?-1:1)*e,2.4*o(),.3*o()-.25,.5-.09*e)}if(e<.72){let e=Math.floor(14*Math.random()),n=Math.floor(3*Math.random()),a=(Math.floor(26*Math.random())/25-.5)*4.4+.02*o();i(6,t,a,(e/13-.5)*2.6+.02*o(),(n-1)*.5+.02*o()-.2,.55+(.09>Math.abs(e/13-.5)?.7:0)+.25*Math.random())}else i(6,t,3.4*o(),2*o(),.5*o()-.3,.1+.12*Math.random());if(e<.58)i(7,t,-2.6+1.7*o(),2.6*o(),1.6*o()-.5,-(.12+.22*Math.random()));else{let e=Math.random();i(7,t,2.5+.16*Math.cos(40*e)+.09*o(),(e-.5)*6.4,.3*o()-.2,.9+.8*e)}if(t<321){let e=t%24,n=Math.floor(t/24);i(8,t,(e-11.5)*.34+.015*o(),(n-6.5)*.42+.015*o(),.4,3)}else i(8,t,(Math.random()-.5)*12,(Math.random()-.5)*8,1.4*o()-1.2,.05+.06*Math.random());if(e<.72){let e=(Math.random()-.5)*6.2;i(9,t,.8+o()*(.16+.22*Math.pow(Math.random(),2)),e,.28*o()-.2,.9+.6*Math.random())}else{let e=Math.random();i(9,t,.8-(1-e)*3.6+.3*o(),2.8*o(),.4*o()-.2,.3+.35*e)}}return a}(u,0,h),v=new Uint16Array(f.length);for(let e=0;e<f.length;e++)v[e]=i.A5E.toHalfFloat(f[e]);let p=new i.IEO(v,512,10*h,i.wk1,i.cLu);p.needsUpdate=!0,p.generateMipmaps=!1,p.minFilter=i.TyD,p.magFilter=i.TyD;let w=new Float32Array(u),g=new Float32Array(u),M=new Float32Array(u),b=new Float32Array(3*u);for(let e=0;e<u;e++)w[e]=e,g[e]=Math.random(),M[e]=.45+1.35*Math.pow(Math.random(),3.2);let x=new i.u9r;x.setAttribute("position",new i.TlE(b,3)),x.setAttribute("aIndex",new i.TlE(w,1)),x.setAttribute("aSeed",new i.TlE(g,1)),x.setAttribute("aSize",new i.TlE(M,1));let y={uForm:{value:p},uTexW:{value:512},uRows:{value:h},uPhase:{value:0},uTime:{value:0},uTurb:{value:0},uPixelRatio:{value:m},uMouse:{value:new i.FM8(99,99)},uMouseForce:{value:0},uIgnite:{value:0},uCtaHeat:{value:0},uPerf:{value:1},uShiftX:{value:o?0:1.9},uRepel:{value:[new i.Ltg(0,0,0,0),new i.Ltg(0,0,0,0),new i.Ltg(0,0,0,0)]},uCool:{value:new i.Ilk("#621307")},uMid:{value:new i.Ilk("#FF5A1F")},uHot:{value:new i.Ilk("#FF8A4C")}},P=new i.jyz({glslVersion:i.LSk,vertexShader:'\n  in float aIndex;\n  in float aSeed;\n  in float aSize;\n\n  uniform sampler2D uForm;      // formations: width TEXW, ROWS rows per formation\n  uniform float uTexW;\n  uniform float uRows;\n  uniform float uPhase;         // 0..9 across the page\n  uniform float uTime;\n  uniform float uTurb;          // scroll-velocity turbulence 0..1\n  uniform float uPixelRatio;\n  uniform vec2  uMouse;         // world xy\n  uniform float uMouseForce;\n  uniform float uIgnite;        // hero entrance 0..1\n  uniform float uCtaHeat;       // CTA hover 0..1\n  uniform float uPerf;          // adaptive throttle 0.55..1\n  uniform float uShiftX;        // formation x-offset so machines live right of the text\n  uniform vec4  uRepel[3];      // NDC rects: xy=center, zw=half-extents\n\n  out float vHeat;\n  out float vAsh;\n  out float vPulse;\n\n  vec4 formation(float f, float idx) {\n    float x = mod(idx, uTexW);\n    float y = floor(idx / uTexW) + f * uRows;\n    return texelFetch(uForm, ivec2(int(x), int(y)), 0);\n  }\n\n  vec3 drift(vec3 p, float t) {\n    return vec3(\n      sin(p.y * 1.7 + t)       * cos(p.z * 1.3 + t * 0.8),\n      sin(p.z * 1.5 + t * 1.1) * cos(p.x * 1.1 + t * 0.9),\n      sin(p.x * 1.3 + t * 0.7) * cos(p.y * 1.9 + t)\n    );\n  }\n\n  float hash(float n) { return fract(sin(n) * 43758.5453123); }\n\n  void main() {\n    float f0 = clamp(floor(uPhase), 0.0, 8.0);\n    float f1 = f0 + 1.0;\n    float blend = smoothstep(0.0, 1.0, uPhase - f0);\n\n    vec4 A = formation(f0, aIndex);\n    vec4 B = formation(f1, aIndex);\n    vec3 pos = mix(A.xyz, B.xyz, blend);\n    float w = mix(A.w, B.w, blend);\n    vAsh = 1.0 - smoothstep(-0.2, 0.0, w);      // negative w = ash population\n    float bright = abs(w);\n\n    // chapter-aware behaviours -------------------------------------------\n    float inDust    = 1.0 - smoothstep(0.0, 1.0, uPhase);\n    float inScatter = smoothstep(0.2, 1.0, uPhase) * (1.0 - smoothstep(1.0, 2.0, uPhase));\n    float inSplit   = smoothstep(6.2, 7.0, uPhase) * (1.0 - smoothstep(7.0, 8.0, uPhase));\n    float inVortex  = smoothstep(8.4, 9.0, uPhase);   // CTA now starts at 8.4 (FAQ owns 8.2-8.4)\n\n    // organic drift — violent in scatter + scroll turbulence, calm in formations.\n    // scatter amp raised + a second de-phased octave so between-slide states stay\n    // haphazard and never settle into an outline of the next block (Ali).\n    // dust (hero/idle) chaos raised (0.14->0.21) + scroll-turbulence response raised\n    // (0.55->0.68) — the beloved chaotic-red-dots field stays MORE alive at rest and\n    // reacts harder to scroll, never calmer (Ali: push the chaos harder, never tame it).\n    float amp = 0.08 + inDust * 0.21 + inScatter * 0.72 + uTurb * 0.68;\n    pos += drift(pos * 0.9 + aSeed * 6.2831, uTime * (0.16 + inScatter * 0.42 + uTurb * 0.4)) * amp;\n    pos += drift(pos * 1.7 + aSeed * 11.3, uTime * (0.31 + inScatter * 0.5)) * (inScatter * 0.34 + uTurb * 0.18);\n    // second-octave buzz — small-scale, fast, dust-only: gives idle embers a living\n    // micro-jitter texture instead of one smooth drift wave (never touches other phases)\n    pos += drift(pos * 2.4 + aSeed * 19.7, uTime * (0.52 + inDust * 0.4)) * inDust * 0.07;\n\n    // scatter chaos: NO synchronized gather (that read as an order-forming shape).\n    // per-particle phase = pure entropy, tiny push only, never a clean contraction.\n    float saw = fract(uTime * 0.09 + hash(aSeed * 91.7) * 1.0);\n    float gather = smoothstep(0.0, 0.6, saw) * (1.0 - smoothstep(0.6, 1.0, saw));\n    pos += (hash(aSeed * 5.1) - 0.5) * gather * inScatter * 0.5;\n\n    // split: ash sinks, the filament rises\n    float dir = mix(1.0, -1.0, vAsh);\n    pos.y += dir * inSplit * (sin(uTime * 0.4 + aSeed * 12.0) * 0.18 + 0.12);\n\n    // slow rotation where the formation is a machine part\n    float spin = (smoothstep(1.6, 2.0, uPhase) * (1.0 - smoothstep(6.4, 7.0, uPhase))) + inVortex;\n    float rot = uTime * 0.08 * spin + inDust * uTime * 0.015;\n    float cr = cos(rot); float sr = sin(rot);\n    pos.xz = mat2(cr, -sr, sr, cr) * pos.xz;\n\n    // the machine formations dock right of the text column\n    float engineWin = smoothstep(1.6, 2.1, uPhase) * (1.0 - smoothstep(5.9, 6.5, uPhase));\n    float constWin  = smoothstep(7.3, 7.9, uPhase) * (1.0 - smoothstep(7.95, 8.15, uPhase));   // fully home before FAQ (8.2)\n    pos.x += (engineWin + constWin) * uShiftX;\n\n    // mouse: living reaction, not a static push — the field is drawn toward the\n    // hand\'s draft then swept around it (real embers near a moving object), never\n    // a clean repel-only vector. Core holds (never collapses to a point), mid-field\n    // swirls tangentially — this is what turns "reacts to cursor" into "a living\n    // ecosystem" per council verdict (reactive ember physics, the #1 move).\n    vec2 toM = pos.xy - uMouse;\n    float md = length(toM);\n    float push = (1.0 - smoothstep(0.0, 1.5, md)) * uMouseForce;\n    vec2 radial = normalize(toM + 0.0001);\n    vec2 tangent = vec2(-radial.y, radial.x);\n    float swirlW = smoothstep(0.12, 1.5, md);       // 0 at the core, 1 by the rim\n    pos.xy += radial * push * 0.5 * (1.0 - swirlW * 0.65);\n    pos.xy += tangent * push * 0.8 * swirlW;\n\n    // hero ignition surge\n    pos *= 0.62 + 0.38 * uIgnite;\n\n    // CTA hover: the ring leans in and heats up\n    pos *= 1.0 - uCtaHeat * inVortex * 0.06;\n\n    // lattice/engine signal pulses\n    float node = floor(aSeed * 2048.0);\n    float phase = fract(hash(node) + uTime * 0.12);\n    float inEngine = smoothstep(1.8, 2.2, uPhase) * (1.0 - smoothstep(6.6, 7.2, uPhase));\n    vPulse = smoothstep(0.0, 0.05, phase) * (1.0 - smoothstep(0.05, 0.2, phase)) * inEngine;\n\n    // two-octave flicker: base breathing + a faster dust-only second octave so the\n    // idle ember field reads as richer/more organic instead of one uniform pulse.\n    float flicker = 0.78 + 0.16 * sin(uTime * (1.4 + hash(aSeed) * 2.6) + aSeed * 40.0)\n                         + 0.10 * inDust * sin(uTime * (3.3 + hash(aSeed * 3.1) * 4.0) + aSeed * 91.0);\n\n    // rare per-particle spark flares — chaotic hot-spots that strobe unpredictably,\n    // biased toward the dust/hero field (Ali\'s "chaotic red dots", pushed harder).\n    float sparkPhase = fract(hash(node * 3.17) + uTime * 0.05);\n    float spark = smoothstep(0.0, 0.02, sparkPhase) * (1.0 - smoothstep(0.02, 0.09, sparkPhase))\n                  * (0.3 + inDust * 0.7);\n\n    vHeat = clamp(bright * flicker + push * 0.7 + uCtaHeat * inVortex * 0.5 + spark * 1.5 + uTurb * 0.5, 0.0, 3.0);\n\n    // cooling transit: the engine dims while it dissolves toward the split\n    // (heat = meaning; also keeps mid-morph glare off the Why text)\n    float transit = smoothstep(6.05, 6.5, uPhase) * (1.0 - smoothstep(6.5, 6.95, uPhase));\n    vHeat *= 1.0 - 0.55 * transit;\n\n    vec4 mv = modelViewMatrix * vec4(pos, 1.0);\n    vec4 clip = projectionMatrix * mv;\n\n    // TEXT IS SACRED AIRSPACE — bend particles out of the measure (NDC rects).\n    // clip.w guard: never touch particles at/behind the camera plane.\n    if (clip.w > 0.0) {\n      vec2 ndc = clip.xy / clip.w;\n      for (int i = 0; i < 3; i++) {\n        vec2 d = abs(ndc - uRepel[i].xy) / max(uRepel[i].zw, vec2(0.0001));\n        float inside = 1.0 - smoothstep(0.8, 1.15, max(d.x, d.y));\n        vec2 away = normalize(ndc - uRepel[i].xy + 0.0001);\n        ndc += away * inside * 0.24 * step(0.011, uRepel[i].z);   // z>0 only when rect active\n      }\n      clip.xy = ndc * clip.w;\n    }\n    gl_Position = clip;\n\n    // fake DOF: distance dims, never balloons (big blurred points read as blobs).\n    // guard: a particle landing exactly on the camera\'s view-space z=0 plane would\n    // divide by zero (gstack_free P1) — clamp the divisor away from zero, invisible\n    // in practice (points are always meaningfully in front of/behind the camera).\n    float focus = smoothstep(3.0, 9.5, -mv.z);\n    float safeDepth = max(abs(-mv.z), 0.05);\n    gl_PointSize = aSize * uPixelRatio * uPerf * (1.0 + vPulse * 2.4 + push * 1.4 + uIgnite * 0.15)\n                   * (5.4 / safeDepth) * (1.0 + focus * 0.4);\n    vHeat *= 1.0 - focus * 0.5;\n  }\n',fragmentShader:"\n  uniform vec3 uCool;   // deep ember\n  uniform vec3 uMid;    // brand orange\n  uniform vec3 uHot;    // white-hot\n  in float vHeat;\n  in float vAsh;\n  in float vPulse;\n  out vec4 fragColor;\n\n  void main() {\n    float d = length(gl_PointCoord - 0.5);\n    // sharp dart: hard bright core, short falloff — no soft bokeh halo\n    float alpha = 1.0 - smoothstep(0.04, 0.34, d);\n    float core  = 1.0 - smoothstep(0.0, 0.13, d);\n    float heat = clamp(vHeat, 0.0, 1.0);\n    vec3 ember = mix(uCool, uMid, smoothstep(0.12, 0.7, heat));\n    ember = mix(ember, uHot, core * min(vHeat * 0.55, 1.2) + vPulse);\n    vec3 ash = vec3(0.29, 0.275, 0.25) * (0.4 + heat * 0.4);\n    vec3 col = mix(ember, ash, vAsh);\n    float a = alpha * (0.4 + heat * 0.6 + vPulse) * (1.0 - vAsh * 0.55);\n    if (a < 0.012) discard;\n    fragColor = vec4(col, a);\n  }\n",uniforms:y,transparent:!0,depthWrite:!1,depthTest:!1,blending:i.WMw}),T=new i.woe(x,P);T.frustumCulled=!1,d.add(T);let A={uTime:y.uTime,uPhase:y.uPhase,uRes:{value:new i.FM8(window.innerWidth*m,window.innerHeight*m)},uMouseUV:{value:new i.FM8(99,99)},uHeat:y.uCtaHeat},C=new i.u9r;C.setAttribute("position",new i.TlE(new Float32Array([-1,-1,0,3,-1,0,-1,3,0]),3));let F=new i.jyz({vertexShader:"\n  void main() { gl_Position = vec4(position, 1.0); }\n",fragmentShader:"\n  uniform float uTime;\n  uniform float uPhase;\n  uniform vec2  uRes;\n  uniform vec2  uMouseUV;\n  uniform float uHeat;\n\n  float hash21(vec2 p) { return fract(sin(dot(p, vec2(127.1, 311.7))) * 43758.5453); }\n  float vnoise(vec2 p) {\n    vec2 i = floor(p); vec2 f = fract(p);\n    vec2 u = f * f * (3.0 - 2.0 * f);\n    return mix(mix(hash21(i), hash21(i + vec2(1, 0)), u.x),\n               mix(hash21(i + vec2(0, 1)), hash21(i + vec2(1, 1)), u.x), u.y);\n  }\n  float fbm(vec2 p) {\n    float v = 0.0; float a = 0.5;\n    for (int i = 0; i < 4; i++) { v += a * vnoise(p); p = p * 2.03 + 17.0; a *= 0.5; }\n    return v;\n  }\n\n  void main() {\n    vec2 uv = gl_FragCoord.xy / uRes;\n    float asp = uRes.x / uRes.y;\n    vec2 p = uv * vec2(asp, 1.0) * 2.2;\n    float t = uTime * 0.028;\n\n    // double domain-warp: the flow itself flows\n    vec2 w1 = vec2(fbm(p + t), fbm(p - t * 0.7));\n    vec2 w2 = vec2(fbm(p + w1 * 1.6 - t * 0.4), fbm(p + w1 * 1.6 + t * 0.55));\n    float m = fbm(p + w2 * 1.8 + vec2(0.0, uPhase * 0.22));\n    float glow = smoothstep(0.48, 0.95, m);\n\n    // molten veins: ridged inversion of the warped field — thin drifting cracks.\n    // Windowed to ignition (hero) and close (CTA) so mid-story stays calm airspace.\n    float ridge = 1.0 - abs(2.0 * fbm(p * 1.35 + w2 * 2.1 - t * 0.5) - 1.0);\n    float veinWin = max(1.0 - smoothstep(0.4, 1.6, uPhase), smoothstep(7.9, 8.8, uPhase));\n    float vein = pow(smoothstep(0.86, 0.995, ridge), 3.0) * veinWin;\n\n    // cursor heat bloom: the field warms under the hand (uMouseUV parks at 99,99\n    // on coarse pointers so this term is zero on touch devices)\n    vec2 md = (uv - uMouseUV) * vec2(asp, 1.0);\n    float mheat = exp(-dot(md, md) * 18.0) * (0.5 + 0.5 * uHeat);\n\n    // heat follows the story: dim through problem, warm at the engine, hot at the close\n    float heat = 0.35 + 0.65 * smoothstep(2.0, 9.0, uPhase);\n    vec3 ember = mix(vec3(0.045, 0.012, 0.005), vec3(0.30, 0.095, 0.028), glow) * heat;\n    ember += vec3(1.0, 0.36, 0.11) * vein * 0.16;\n    ember += vec3(0.42, 0.13, 0.04) * mheat * heat;\n\n    // shader-side animated grain: de-bands the deep gradient, reads as film\n    float g = (hash21(uv * uRes + fract(uTime) * 61.7) - 0.5) * 0.012;\n\n    float vig = 1.0 - smoothstep(0.35, 1.3, length(uv - 0.5) * 2.0);\n    gl_FragColor = vec4(ember * vig + g, 1.0);\n  }\n",uniforms:A,depthWrite:!1,depthTest:!1}),k=new i.Kj0(C,F);k.frustumCulled=!1,k.renderOrder=-1,d.add(k);let z=function(e,t){let n=new i.cJO(1.2,t),a=n.attributes.position,o=new i.Pa4,r=(e,t,n)=>{let a=43758.5453123*Math.sin(127.1*e+311.7*t+74.7*n);return a-Math.floor(a)},s=(e,t,n)=>{let a=Math.floor(e),o=Math.floor(t),s=Math.floor(n),l=e-a,u=t-o,h=n-s,m=l*l*(3-2*l),d=u*u*(3-2*u),c=r(a,o,s),f=r(a+1,o,s),v=r(a,o+1,s),p=r(a+1,o+1,s),w=r(a,o,s+1),g=r(a+1,o,s+1),M=r(a,o+1,s+1),b=r(a+1,o+1,s+1),x=i.M8C.lerp(c,f,m),y=i.M8C.lerp(v,p,m),P=i.M8C.lerp(w,g,m),T=i.M8C.lerp(M,b,m),A=i.M8C.lerp(x,y,d),C=i.M8C.lerp(P,T,d);return i.M8C.lerp(A,C,h*h*(3-2*h))},l=(e,t,n)=>{let a=0,o=.5,i=1;for(let r=0;r<4;r++)a+=o*s(e*i,t*i,n*i),i*=2.02,o*=.5;return a},u=new Float32Array(a.count);for(let e=0;e<a.count;e++){o.fromBufferAttribute(a,e);let t=o.clone().normalize(),n=l(1.8*t.x,1.8*t.y,1.8*t.z);o.copy(t).multiplyScalar(1.2*(1+(n-.5)*.045)),a.setXYZ(e,o.x,o.y,o.z);let i=1-Math.abs(2*l(2.6*t.x+9,2.6*t.y+9,2.6*t.z+9)-1);u[e]=Math.pow(Math.max(0,(i-.88)/.115),3)}return n.setAttribute("aCrack",new i.TlE(u,1)),n.computeVertexNormals(),n}(0,o?3:4),S={uTime:y.uTime,uOpacity:{value:0},uBase:{value:new i.Ilk(.075,.05,.045)},uMid:{value:new i.Ilk("#FF5A1F")},uHot:{value:new i.Ilk("#FF8A4C")},uLightDir:{value:new i.Pa4(.5,.75,.85).normalize()}},H=new i.jyz({vertexShader:"\n  attribute float aCrack;\n  varying float vCrack;\n  varying vec3 vNormal;\n  varying vec3 vViewPosition;\n  void main() {\n    vCrack = aCrack;\n    vNormal = normalize(normalMatrix * normal);\n    vec4 mv = modelViewMatrix * vec4(position, 1.0);\n    vViewPosition = -mv.xyz;\n    gl_Position = projectionMatrix * mv;\n  }\n",fragmentShader:"\n  uniform float uTime;\n  uniform float uOpacity;\n  uniform vec3 uBase;\n  uniform vec3 uMid;\n  uniform vec3 uHot;\n  uniform vec3 uLightDir;\n  varying float vCrack;\n  varying vec3 vNormal;\n  varying vec3 vViewPosition;\n\n  void main() {\n    vec3 N = normalize(vNormal);\n    vec3 V = normalize(vViewPosition);\n    vec3 L = normalize(uLightDir);\n    float diff = max(dot(N, L), 0.0);\n    vec3 H = normalize(L + V);\n    float spec = pow(max(dot(N, H), 0.0), 42.0);\n    float fresnel = pow(1.0 - max(dot(N, V), 0.0), 2.6);\n\n    // molten crack veins — slow ember flicker, never a uniform pulse\n    float flick = 0.72 + 0.28 * sin(uTime * 1.7 + vCrack * 34.0);\n    vec3 crackCol = mix(uMid, uHot, smoothstep(0.45, 1.0, vCrack)) * vCrack * flick * 2.1;\n\n    vec3 base = uBase * (0.24 + diff * 0.66);\n    vec3 col = base + crackCol + fresnel * uMid * 0.5 + spec * uHot * 0.85;\n\n    gl_FragColor = vec4(col, uOpacity);\n  }\n",uniforms:S,transparent:!0,depthWrite:!0,depthTest:!0}),E=new i.Kj0(z,H);E.renderOrder=1,E.position.set(1.85,.12,-.4);let R=[],W=()=>{let e=Array.from(document.querySelectorAll("[data-forge]")),t=document.documentElement.scrollHeight,n=window.innerHeight;e.forEach((e,a)=>{let[o,i]=(e.dataset.forge||"0:0").split(":").map(Number),r=e.offsetTop+e.offsetHeight>=t-.5*n;R.push(l.i.create({trigger:e,start:0===a?"top top":"top bottom",end:r?"bottom bottom":"bottom top",scrub:.7,onUpdate:e=>{y.uPhase.value=o+(i-o)*e.progress}}))})},L=0;L=requestAnimationFrame(()=>{L=requestAnimationFrame(()=>{W(),l.i.refresh()})}),null===(t=document.fonts)||void 0===t||t.ready.then(()=>{Z(),l.i.refresh()}),s.ZP.to(y.uIgnite,{value:1,duration:2.6,ease:"power3.out",delay:.15});let I=window.scrollY,V=new i.FM8(99,99),D=new i.FM8(99,99),N=0,_=0,O=0,j=e=>{let t=e.clientX/window.innerWidth*2-1,n=-(e.clientY/window.innerHeight*2)+1,a=14.4*Math.tan(50*Math.PI/360),o=window.innerWidth/window.innerHeight*a;V.set(t*o/2,n*a/2),D.set(e.clientX/window.innerWidth,1-e.clientY/window.innerHeight),O=Math.min(1,O+.0045*Math.hypot(e.clientX-N,e.clientY-_)),N=e.clientX,_=e.clientY};o||window.addEventListener("pointermove",j,{passive:!0});let X=document.querySelector("[data-cta-heat]"),q=()=>s.ZP.to(y.uCtaHeat,{value:1,duration:.7,ease:"power2.out"}),B=()=>s.ZP.to(y.uCtaHeat,{value:0,duration:1.1,ease:"power2.out"});null==X||X.addEventListener("mouseenter",q),null==X||X.addEventListener("mouseleave",B);let U=()=>Array.from(document.querySelectorAll("[data-repel]")),Y=[],Z=()=>{Y=U()};Z();let K=()=>{let e=0,t=window.innerWidth,n=window.innerHeight;for(let a of Y){if(e>=3)break;let o=a.getBoundingClientRect();!(o.bottom<0)&&!(o.top>n)&&(y.uRepel.value[e].set((o.left+o.right)/2/t*2-1,-((o.top+o.bottom)/2/n*2-1),Math.max(o.width/t,.01),Math.max(o.height/n,.01)),e++)}for(;e<3;e++)y.uRepel.value[e].set(0,0,0,0)},Q=0,J=!0,G=!1,$=0,ee=0,et=0,en=()=>{if(!J||G)return;let e=performance.now();if(ee>0){let t=e-ee;y.uTime.value+=Math.min(t,50)/1e3,t>22?et++:et=Math.max(0,et-1),et>30&&y.uPerf.value>.55?(y.uPerf.value*=.95,et=15):0===et&&y.uPerf.value<1&&(y.uPerf.value=Math.min(1,y.uPerf.value+.002))}ee=e;let t=window.scrollY;y.uTurb.value=Math.min(1,.92*y.uTurb.value+.0016*Math.abs(t-I)),I=t,y.uMouse.value.lerp(V,.08),A.uMouseUV.value.lerp(D,.06),O*=.94,y.uMouseForce.value+=(O-y.uMouseForce.value)*.1,(3&$++)==0&&K();let a=1-i.M8C.smoothstep(y.uPhase.value,.5,.95);S.uOpacity.value=o?0:y.uIgnite.value*a,E.rotation.y=.1*y.uTime.value+.5*y.uPhase.value,E.rotation.x=.09*Math.sin(.09*y.uTime.value)+.025*y.uMouse.value.y,E.rotation.z=.018*y.uMouse.value.x,E.position.y=.12+.09*Math.sin(.32*y.uTime.value);let r=y.uPhase.value;c.position.z=7.2-.35*Math.min(r,2)+.1*Math.sin(.11*y.uTime.value),c.position.x=.35*Math.sin(.7*r)+.02*y.uMouse.value.x,c.position.y=.2*Math.cos(.5*r)+.02*y.uMouse.value.y,c.lookAt(0,0,0),n.render(d,c),Q=requestAnimationFrame(en)};Q=requestAnimationFrame(en);let ea=()=>{(J="visible"===document.visibilityState)&&!G?(ee=0,Q=requestAnimationFrame(en)):cancelAnimationFrame(Q)};document.addEventListener("visibilitychange",ea);let eo=()=>{m=Math.min(window.devicePixelRatio||1,1.5),n.setPixelRatio(m),y.uPixelRatio.value=m,c.aspect=window.innerWidth/window.innerHeight,c.updateProjectionMatrix(),n.setSize(window.innerWidth,window.innerHeight),A.uRes.value.set(window.innerWidth*m,window.innerHeight*m),Z()};window.addEventListener("resize",eo);let ei=n.domElement,er=e=>{e.preventDefault(),G=!0,cancelAnimationFrame(Q)},es=()=>{G=!1,Q=requestAnimationFrame(en)};return ei.addEventListener("webglcontextlost",er),ei.addEventListener("webglcontextrestored",es),()=>{J=!1,cancelAnimationFrame(Q),cancelAnimationFrame(L),R.forEach(e=>e.kill()),document.removeEventListener("visibilitychange",ea),window.removeEventListener("resize",eo),o||window.removeEventListener("pointermove",j),null==X||X.removeEventListener("mouseenter",q),null==X||X.removeEventListener("mouseleave",B),ei.removeEventListener("webglcontextlost",er),ei.removeEventListener("webglcontextrestored",es),x.dispose(),P.dispose(),C.dispose(),F.dispose(),p.dispose(),z.dispose(),H.dispose(),n.dispose(),ei.parentElement===a&&a.removeChild(ei)}},[]),(0,a.jsx)("div",{ref:e,"aria-hidden":!0,className:"pointer-events-none fixed inset-0 z-0",style:{contain:"strict"}})}}}]);